name: 'Public Infrastructure Action PoC'
description:
  'A proof of concept for GitHub workflow-to-workflow interactions, outputs, and
  results.'
author: 'Your name or organization here'

# outputs:
#   from_jobs:
#     description: An object, where the keys are the names of the prior jobs and value is a JSON string representing its output.

runs:
  using: node20
  main: dist/index.js
